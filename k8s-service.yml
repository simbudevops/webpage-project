apiVersion: v1
kind: Service
metadata:
  name: raegan-service-iam
  namespace: default
  labels:
    app: raegan-app-iam
spec:
  type: NodePort
  selector:
    app: raegan-app-iam
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
